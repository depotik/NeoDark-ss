import os
import sys
import time
import random
from datetime import datetime

def print_header():
    """Выводит заголовок программы"""
    print("🎵 Консольный плеер (Demo)")
    print("=" * 40)

def get_neodark_banner():
    """Возвращает баннер NeoDark"""
    return '''\033[96m███╗   ██╗███████╗ ██████╗ ██████╗  █████╗ ██████╗ ██╗  ██╗
████╗  ██║██╔════╝██╔═══██╗██╔══██╗██╔══██╗██╔══██╗██║ ██╔╝
██╔██╗ ██║█████╗  ██║   ██║██║  ██║███████║██████╔╝█████╔╝ 
██║╚██╗██║██╔══╝  ██║   ██║██║  ██║██╔══██║██╔══██╗██╔═██╗ 
██║ ╚████║███████╗╚██████╔╝██████╔╝██║  ██║██║  ██║██║  ██╗
╚═╝  ╚═══╝╚══════╝ ╚═════╝ ╚═════╝ ╚═╝  ╚═╝╚═╝  ╚═╝╚═╝  ╚═╝\033[0m'''

def show_neodark_logo():
    """Показывает логотип NeoDark"""
    print(get_neodark_banner())
    print()

def show_player_info():
    """Показывает информацию о консольном плеере"""
    print("ℹ️ Консольный плеер NeoDark:")
    print("-" * 35)
    print("   Консольный плеер позволяет")
    print("   воспроизводить аудио и видео")
    print("   файлы прямо в терминале.")
    print()
    print("   Поддерживаемые форматы:")
    print("   • MP3, WAV, OGG (аудио)")
    print("   • MP4, AVI, MKV (видео)")
    print("   • FLAC, AAC, M4A")
    print("   • Playlists (M3U, PLS)")

def show_demo_player():
    """Показывает демонстрацию плеера"""
    print("\n🎵 Демонстрация плеера:")
    print("-" * 30)
    
    try:
        # Симуляция библиотеки музыки
        tracks = [
            {"title": "NeoDark Theme", "artist": "System", "album": "NeoDark OST", "duration": "3:45"},
            {"title": "Cyber Wave", "artist": "Digital Dreams", "album": "Future Sounds", "duration": "4:22"},
            {"title": "Matrix Rain", "artist": "Code Symphony", "album": "Digital World", "duration": "5:18"},
            {"title": "Tech Noir", "artist": "Neon Lights", "album": "Synthwave Collection", "duration": "3:56"},
            {"title": "Binary Sunset", "artist": "Byte Beats", "album": "Algorithmic Melodies", "duration": "4:33"}
        ]
        
        print("🎶 Музыкальная библиотека:")
        print("-" * 35)
        for i, track in enumerate(tracks, 1):
            print(f"   [{i}] {track['title']}")
            print(f"       {track['artist']} - {track['album']} ({track['duration']})")
            print()
        
        # Выбор трека для воспроизведения
        print("Выберите трек для воспроизведения (1-5):")
        choice = input("Ваш выбор: ").strip()
        
        if choice.isdigit() and 1 <= int(choice) <= len(tracks):
            selected_track = tracks[int(choice) - 1]
            print(f"\n▶️  Воспроизведение: {selected_track['title']}")
            print(f"   Исполнитель: {selected_track['artist']}")
            print(f"   Альбом: {selected_track['album']}")
            print(f"   Длительность: {selected_track['duration']}")
            
            # Симуляция воспроизведения
            print("\n🔄 Воспроизведение...")
            duration_parts = selected_track['duration'].split(':')
            total_seconds = int(duration_parts[0]) * 60 + int(duration_parts[1])
            
            # Показываем прогресс-бар
            for i in range(total_seconds):
                progress = (i / total_seconds) * 100
                bar_length = 30
                filled_length = int(bar_length * i // total_seconds)
                bar = '█' * filled_length + '░' * (bar_length - filled_length)
                
                # Генерируем случайные данные для визуализации
                visualization = ''.join(random.choices('◆◇◈◉◎●', k=10))
                
                print(f"\r[{bar}] {progress:.1f}% | {visualization} | {i//60:02d}:{i%60:02d}", end='')
                time.sleep(0.1)  # Замедляем для демонстрации
            
            print(f"\n✅ Воспроизведение завершено!")
            return True
        else:
            print("❌ Неверный выбор")
            return False
            
    except Exception as e:
        print(f"❌ Ошибка воспроизведения: {e}")
        return False

def show_player_features():
    """Показывает функции плеера"""
    print("\n🎛️ Функции плеера:")
    print("-" * 20)
    print("   [1] Воспроизведение")
    print("   [2] Пауза/Продолжить")
    print("   [3] Следующий трек")
    print("   [4] Предыдущий трек")
    print("   [5] Громкость")
    print("   [6] Плейлисты")
    print("   [7] Эквалайзер")
    print("   [8] Визуализация")

def show_player_statistics():
    """Показывает статистику плеера"""
    print("\n📊 Статистика плеера:")
    print("-" * 25)
    print("   Всего треков: 142")
    print("   Всего альбомов: 27")
    print("   Всего исполнителей: 58")
    print("   Общее время: 9ч 32мин")
    print("   Любимых треков: 23")
    print("   Плейлистов: 7")

def main():
    """Главная функция консольного плеера"""
    # Очищаем экран
    os.system('cls' if os.name == 'nt' else 'clear')
    
    # Показываем логотип и заголовок
    show_neodark_logo()
    print_header()
    
    try:
        # Показываем информацию
        show_player_info()
        
        # Показываем функции
        show_player_features()
        
        # Показываем статистику
        show_player_statistics()
        
        # Демонстрация плеера
        print("\n" + "=" * 40)
        if show_demo_player():
            print("\n🎉 Демонстрация плеера завершена!")
        else:
            print("\n⚠️  Демонстрация не завершена")
        
        print(f"\n✅ Работа завершена!")
        print(f"⏰ Время завершения: {datetime.now().strftime('%Y-%m-%d %H:%M:%S')}")
        
    except KeyboardInterrupt:
        print(f"\n\n⚠️  Демонстрация прервана пользователем")
    except Exception as e:
        print(f"\n❌ Произошла ошибка: {str(e)}")
    
    input("\nНажмите Enter для выхода...")

if __name__ == "__main__":
    main()