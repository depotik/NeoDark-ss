import os
import sys
import time
import random
from datetime import datetime

def print_header():
    """Выводит заголовок программы"""
    print("🔍 Трассировка до продукта")
    print("=" * 35)

def get_neodark_banner():
    """Возвращает баннер NeoDark"""
    return '''\033[96m███╗   ██╗███████╗ ██████╗ ██████╗  █████╗ ██████╗ ██╗  ██╗
████╗  ██║██╔════╝██╔═══██╗██╔══██╗██╔══██╗██╔══██╗██║ ██╔╝
██╔██╗ ██║█████╗  ██║   ██║██║  ██║███████║██████╔╝█████╔╝ 
██║╚██╗██║██╔══╝  ██║   ██║██║  ██║██╔══██║██╔══██╗██╔═██╗ 
██║ ╚████║███████╗╚██████╔╝██████╔╝██║  ██║██║  ██║██║  ██╗
╚═╝  ╚═══╝╚══════╝ ╚═════╝ ╚═════╝ ╚═╝  ╚═╝╚═╝  ╚═╝╚═╝  ╚═╝\033[0m'''

def show_neodark_logo():
    """Показывает логотип NeoDark"""
    print(get_neodark_banner())
    print()

def show_traceroute_info():
    """Показывает информацию о трассировке"""
    print("ℹ️ Трассировка до продукта:")
    print("-" * 35)
    print("   Трассировка показывает путь, который")
    print("   проходят сетевые пакеты от вашего")
    print("   компьютера до серверов NeoDark.")
    print()
    print("   Позволяет определить:")
    print("   • Проблемы с сетью")
    print("   • Задержки на маршруте")
    print("   • Местоположение узлов")
    print("   • Качество соединения")

def run_product_traceroute_demo():
    """Демонстрация трассировки до продукта"""
    print("\n🔍 Трассировка до продукта (демо):")
    print("-" * 40)
    
    try:
        # Выбор продукта для трассировки
        print("Выберите продукт для трассировки:")
        products = {
            "1": "core.neodark.ru",
            "2": "cloud.neodark.ru", 
            "3": "api.neodark.ru",
            "4": "updates.neodark.ru"
        }
        
        for key, value in products.items():
            print(f"   [{key}] {value}")
        
        choice = input("\nВаш выбор (1-4): ").strip()
        if choice not in products:
            choice = "1"
        
        target = products[choice]
        print(f"Цель трассировки: {target}")
        
        # Имитация трассировки
        print(f"\n🔄 Трассировка до {target}...")
        
        # Генерируем случайные данные для трассировки
        hops = random.randint(8, 15)
        
        print("Шаг  IP-адрес           Имя хоста              Время")
        print("-" * 55)
        
        for i in range(1, hops + 1):
            # Генерируем данные для каждого шага
            ip_parts = [str(random.randint(1, 255)) for _ in range(4)]
            ip = ".".join(ip_parts)
            
            if i <= 3:
                hostname = f"router{i}.local.isp.net"
            elif i <= hops - 3:
                hostname = f"core{i-3}.backbone.net"
            elif i == hops:
                hostname = target
            else:
                hostname = f"edge{i}.isp.net"
            
            # Генерируем время отклика
            if i == hops:
                time_ms = random.randint(20, 100)
            elif i <= 3:
                time_ms = random.randint(1, 10)
            else:
                time_ms = random.randint(5, 50)
            
            print(f"{i:2d}   {ip:<15} {hostname:<22} {time_ms}ms")
            time.sleep(0.3)  # Задержка для реалистичности
        
        print(f"\n✅ Трассировка завершена!")
        print(f"📊 Всего шагов: {hops}")
        print(f"⏱️  Общее время: {random.randint(50, 200)}ms")
        
        return True
        
    except Exception as e:
        print(f"❌ Ошибка трассировки: {e}")
        return False

def show_traceroute_tips():
    """Показывает советы по трассировке"""
    print("\n💡 Советы по трассировке:")
    print("-" * 30)
    print("   • Используйте трассировку для")
    print("     диагностики сетевых проблем")
    print("   • Обращайте внимание на большие")
    print("     задержки и потери пакетов")
    print("   • Сравнивайте трассировки в")
    print("     разное время суток")
    print("   • Используйте несколько попыток")
    print("     для получения точных данных")

def show_network_diagnostics():
    """Показывает инструменты диагностики сети"""
    print("\n🛠️ Инструменты диагностики:")
    print("-" * 30)
    print("   [1] Ping - проверка доступности")
    print("   [2] Traceroute - трассировка пути")
    print("   [3] Netstat - сетевые соединения")
    print("   [4] Nslookup - DNS-запросы")
    print("   [5] Ipconfig - конфигурация сети")

def main():
    """Главная функция трассировки до продукта"""
    # Очищаем экран
    os.system('cls' if os.name == 'nt' else 'clear')
    
    # Показываем логотип и заголовок
    show_neodark_logo()
    print_header()
    
    try:
        # Показываем информацию
        show_traceroute_info()
        
        # Показываем советы
        show_traceroute_tips()
        
        # Показываем инструменты диагностики
        show_network_diagnostics()
        
        # Демонстрация трассировки
        print("\n" + "=" * 35)
        if run_product_traceroute_demo():
            print("\n🎉 Трассировка завершена успешно!")
        else:
            print("\n⚠️  Трассировка не завершена")
        
        print(f"\n✅ Работа завершена!")
        print(f"⏰ Время завершения: {datetime.now().strftime('%Y-%m-%d %H:%M:%S')}")
        
    except KeyboardInterrupt:
        print(f"\n\n⚠️  Трассировка прервана пользователем")
    except Exception as e:
        print(f"\n❌ Произошла ошибка: {str(e)}")
    
    input("\nНажмите Enter для выхода...")

if __name__ == "__main__":
    main()