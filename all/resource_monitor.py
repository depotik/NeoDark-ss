import os
import sys
import time
import random
from datetime import datetime

def print_header():
    """Выводит заголовок программы"""
    print("📊 Мониторинг ресурсов (график CLI)")
    print("=" * 45)

def get_neodark_banner():
    """Возвращает баннер NeoDark"""
    return '''\033[96m███╗   ██╗███████╗ ██████╗ ██████╗  █████╗ ██████╗ ██╗  ██╗
████╗  ██║██╔════╝██╔═══██╗██╔══██╗██╔══██╗██╔══██╗██║ ██╔╝
██╔██╗ ██║█████╗  ██║   ██║██║  ██║███████║██████╔╝█████╔╝ 
██║╚██╗██║██╔══╝  ██║   ██║██║  ██║██╔══██║██╔══██╗██╔═██╗ 
██║ ╚████║███████╗╚██████╔╝██████╔╝██║  ██║██║  ██║██║  ██╗
╚═╝  ╚═══╝╚══════╝ ╚═════╝ ╚═════╝ ╚═╝  ╚═╝╚═╝  ╚═╝╚═╝  ╚═╝\033[0m'''

def show_neodark_logo():
    """Показывает логотип NeoDark"""
    print(get_neodark_banner())
    print()

def show_monitor_info():
    """Показывает информацию о мониторинге ресурсов"""
    print("ℹ️ Мониторинг ресурсов:")
    print("-" * 30)
    print("   Мониторинг ресурсов позволяет")
    print("   отслеживать использование:")
    print("   • Центрального процессора")
    print("   • Оперативной памяти")
    print("   • Дискового пространства")
    print("   • Сетевой активности")
    print()
    print("   Предоставляется в виде графиков")
    print("   и таблиц в реальном времени.")

def show_resource_monitor():
    """Показывает монитор ресурсов"""
    print("\n📊 Монитор ресурсов:")
    print("-" * 30)
    
    try:
        print("🔄 Мониторинг ресурсов в реальном времени...")
        print("Нажмите Ctrl+C для выхода")
        print()
        
        # Симуляция мониторинга ресурсов
        for cycle in range(20):  # 20 циклов по 0.5 секунды = 10 секунд
            # Генерируем случайные значения для ресурсов
            cpu_usage = random.randint(5, 95)
            memory_usage = random.randint(20, 85)
            disk_usage = random.randint(30, 75)
            network_usage = random.randint(10, 100)
            
            # Создаем графики
            cpu_bar = create_bar(cpu_usage, 20, '█', '░')
            memory_bar = create_bar(memory_usage, 20, '█', '░')
            disk_bar = create_bar(disk_usage, 20, '█', '░')
            network_bar = create_bar(network_usage, 20, '█', '░')
            
            # Очищаем экран и показываем обновленные данные
            os.system('cls' if os.name == 'nt' else 'clear')
            
            # Показываем заголовок
            show_neodark_logo()
            print_header()
            print("📊 Монитор ресурсов (демо):")
            print("-" * 45)
            
            # Показываем ресурсы с графиками
            print(f"💻 CPU Usage    [{cpu_bar}] {cpu_usage:3d}%")
            print(f"🧠 Memory       [{memory_bar}] {memory_usage:3d}%")
            print(f"💾 Disk         [{disk_bar}] {disk_usage:3d}%")
            print(f"🌐 Network      [{network_bar}] {network_usage:3d}%")
            
            # Показываем дополнительную информацию
            print()
            print("📈 Дополнительная информация:")
            print(f"   Время работы: {cycle//2} секунд")
            print(f"   Пик CPU: {max(cpu_usage, 75)}%")
            print(f"   Свободная память: {100-memory_usage}%")
            print(f"   Температура: {random.randint(45, 65)}°C")
            
            # Ждем перед следующим обновлением
            time.sleep(0.5)
        
        print("\n✅ Мониторинг завершен!")
        return True
        
    except KeyboardInterrupt:
        print("\n⚠️ Мониторинг остановлен пользователем")
        return True
    except Exception as e:
        print(f"❌ Ошибка мониторинга: {e}")
        return False

def create_bar(percentage, width, filled_char, empty_char):
    """Создает текстовый индикатор прогресса"""
    filled_width = int(width * percentage / 100)
    empty_width = width - filled_width
    return filled_char * filled_width + empty_char * empty_width

def show_monitor_features():
    """Показывает функции монитора"""
    print("\n🎛️ Функции монитора:")
    print("-" * 25)
    print("   [1] Мониторинг CPU")
    print("   [2] Мониторинг памяти")
    print("   [3] Мониторинг диска")
    print("   [4] Мониторинг сети")
    print("   [5] Все ресурсы")
    print("   [6] История использования")
    print("   [7] Экспорт данных")
    print("   [8] Уведомления")

def show_monitor_statistics():
    """Показывает статистику мониторинга"""
    print("\n📊 Статистика системы:")
    print("-" * 25)
    print("   Среднее использование CPU: 42%")
    print("   Пик CPU за час: 87%")
    print("   Среднее использование памяти: 58%")
    print("   Пик памяти за час: 78%")
    print("   Передача данных: 2.4 GB")
    print("   Ошибок сети: 0")

def main():
    """Главная функция монитора ресурсов"""
    # Очищаем экран
    os.system('cls' if os.name == 'nt' else 'clear')
    
    # Показываем логотип и заголовок
    show_neodark_logo()
    print_header()
    
    try:
        # Показываем информацию
        show_monitor_info()
        
        # Показываем функции
        show_monitor_features()
        
        # Показываем статистику
        show_monitor_statistics()
        
        # Мониторинг ресурсов
        print("\n" + "=" * 45)
        if show_resource_monitor():
            print("\n🎉 Мониторинг ресурсов завершен!")
        else:
            print("\n⚠️  Мониторинг не завершен")
        
        print(f"\n✅ Работа завершена!")
        print(f"⏰ Время завершения: {datetime.now().strftime('%Y-%m-%d %H:%M:%S')}")
        
    except KeyboardInterrupt:
        print(f"\n\n⚠️  Мониторинг прерван пользователем")
    except Exception as e:
        print(f"\n❌ Произошла ошибка: {str(e)}")
    
    input("\nНажмите Enter для выхода...")

if __name__ == "__main__":
    main()