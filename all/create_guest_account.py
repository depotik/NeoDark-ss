import os
import sys
import random
import string
from datetime import datetime, timedelta

def print_header():
    """Выводит заголовок программы"""
    print("👥 Создание гостевого аккаунта")
    print("=" * 40)

def get_neodark_banner():
    """Возвращает баннер NeoDark"""
    return '''\033[96m███╗   ██╗███████╗ ██████╗ ██████╗  █████╗ ██████╗ ██╗  ██╗
████╗  ██║██╔════╝██╔═══██╗██╔══██╗██╔══██╗██╔══██╗██║ ██╔╝
██╔██╗ ██║█████╗  ██║   ██║██║  ██║███████║██████╔╝█████╔╝ 
██║╚██╗██║██╔══╝  ██║   ██║██║  ██║██╔══██║██╔══██╗██╔═██╗ 
██║ ╚████║███████╗╚██████╔╝██████╔╝██║  ██║██║  ██║██║  ██╗
╚═╝  ╚═══╝╚══════╝ ╚═════╝ ╚═════╝ ╚═╝  ╚═╝╚═╝  ╚═╝╚═╝  ╚═╝\033[0m'''

def show_neodark_logo():
    """Показывает логотип NeoDark"""
    print(get_neodark_banner())
    print()

def generate_guest_credentials():
    """Генерирует учетные данные для гостевого аккаунта"""
    # Генерируем имя пользователя
    guest_prefix = "guest_"
    random_suffix = ''.join(random.choices(string.ascii_lowercase + string.digits, k=6))
    username = guest_prefix + random_suffix
    
    # Генерируем пароль
    password = ''.join(random.choices(string.ascii_letters + string.digits + "!@#$%", k=12))
    
    # Генерируем срок действия (7 дней)
    expiry_date = datetime.now() + timedelta(days=7)
    
    return username, password, expiry_date

def create_guest_account_demo():
    """Демонстрация создания гостевого аккаунта"""
    print("👤 Создание гостевого аккаунта (демо):")
    print("-" * 40)
    
    try:
        # Получаем параметры аккаунта
        print("   Параметры гостевого аккаунта:")
        guest_name = input("   Имя гостя (необязательно): ").strip()
        
        # Генерируем учетные данные
        print("   🔄 Генерация учетных данных...")
        username, password, expiry_date = generate_guest_credentials()
        
        # Имитация создания аккаунта
        import time
        steps = [
            "Проверка доступности имени пользователя",
            "Генерация безопасного пароля",
            "Настройка ограничений доступа",
            "Создание профиля пользователя",
            "Установка срока действия"
        ]
        
        for i, step in enumerate(steps, 1):
            print(f"   [{i}/{len(steps)}] {step}...")
            time.sleep(0.5)
        
        # Показываем учетные данные
        print("\n   ✅ Гостевой аккаунт создан успешно!")
        print(f"   👤 Имя пользователя: {username}")
        print(f"   🔐 Пароль: {password}")
        print(f"   📅 Действует до: {expiry_date.strftime('%Y-%m-%d %H:%M')}")
        
        if guest_name:
            print(f"   🏷️  Гость: {guest_name}")
        
        # Предупреждение о безопасности
        print("\n   ⚠️  Важно:")
        print("   • Передайте учетные данные гостю напрямую")
        print("   • Не сохраняйте их в незащищенных местах")
        print("   • Аккаунт будет автоматически удален по истечении срока")
        
        return True
        
    except Exception as e:
        print(f"   ❌ Ошибка создания гостевого аккаунта: {e}")
        return False

def show_guest_restrictions():
    """Показывает ограничения гостевого аккаунта"""
    print("\n🔒 Ограничения гостевого аккаунта:")
    print("-" * 40)
    print("   • Доступ только к базовым функциям")
    print("   • Невозможность изменения настроек системы")
    print("   • Ограниченный доступ к файлам")
    print("   • Запрет на установку программ")
    print("   • Ограничение по времени использования")
    print("   • Невозможность доступа к аккаунтам других пользователей")
    print("   • Логирование всей активности")

def show_guest_features():
    """Показывает функции гостевого аккаунта"""
    print("\n✨ Функции гостевого аккаунта:")
    print("-" * 35)
    print("   • Просмотр документов")
    print("   • Доступ к интернету")
    print("   • Использование офисных приложений")
    print("   • Печать документов")
    print("   • Доступ к медиафайлам")
    print("   • Использование браузера")

def main():
    """Главная функция создания гостевого аккаунта"""
    # Очищаем экран
    os.system('cls' if os.name == 'nt' else 'clear')
    
    # Показываем логотип и заголовок
    show_neodark_logo()
    print_header()
    
    try:
        # Демонстрация создания гостевого аккаунта
        if create_guest_account_demo():
            print("\n🎉 Гостевой аккаунт успешно создан!")
        else:
            print("\n⚠️  Создание гостевого аккаунта не выполнено")
        
        # Показываем ограничения и функции
        show_guest_restrictions()
        show_guest_features()
        
        print(f"\n✅ Работа завершена!")
        print(f"⏰ Время завершения: {datetime.now().strftime('%Y-%m-%d %H:%M:%S')}")
        
    except KeyboardInterrupt:
        print(f"\n\n⚠️  Операция прервана пользователем")
    except Exception as e:
        print(f"\n❌ Произошла ошибка: {str(e)}")
    
    input("\nНажмите Enter для выхода...")

if __name__ == "__main__":
    main()