import os
import sys
import time
import random
from datetime import datetime

def print_header():
    """Выводит заголовок программы"""
    print("🛡️ Проверка IP-репутации")
    print("=" * 35)

def get_neodark_banner():
    """Возвращает баннер NeoDark"""
    return '''\033[96m███╗   ██╗███████╗ ██████╗ ██████╗  █████╗ ██████╗ ██╗  ██╗
████╗  ██║██╔════╝██╔═══██╗██╔══██╗██╔══██╗██╔══██╗██║ ██╔╝
██╔██╗ ██║█████╗  ██║   ██║██║  ██║███████║██████╔╝█████╔╝ 
██║╚██╗██║██╔══╝  ██║   ██║██║  ██║██╔══██║██╔══██╗██╔═██╗ 
██║ ╚████║███████╗╚██████╔╝██████╔╝██║  ██║██║  ██║██║  ██╗
╚═╝  ╚═══╝╚══════╝ ╚═════╝ ╚═════╝ ╚═╝  ╚═╝╚═╝  ╚═╝╚═╝  ╚═╝\033[0m'''

def show_neodark_logo():
    """Показывает логотип NeoDark"""
    print(get_neodark_banner())
    print()

def show_reputation_info():
    """Показывает информацию о проверке репутации IP"""
    print("ℹ️ Проверка IP-репутации:")
    print("-" * 35)
    print("   Проверка репутации IP-адреса")
    print("   позволяет определить, не занесен")
    print("   ли адрес в черные списки.")
    print()
    print("   Проверяются сервисы:")
    print("   • VirusTotal")
    print("   • AbuseIPDB")
    print("   • Spamhaus")
    print("   • Emerging Threats")
    print("   • McAfee")

def check_ip_reputation_demo():
    """Демонстрация проверки IP-репутации"""
    print("\n🔍 Проверка IP-репутации (демо):")
    print("-" * 40)
    
    try:
        # Получаем IP-адрес для проверки
        ip_address = input("Введите IP-адрес для проверки (или оставьте пустым): ").strip()
        if not ip_address:
            ip_address = "192.168.1.100"
            print(f"Используется IP: {ip_address}")
        
        # Имитация проверки
        print(f"\n🔄 Проверка репутации IP {ip_address}...")
        
        services = [
            "VirusTotal",
            "AbuseIPDB", 
            "Spamhaus",
            "Emerging Threats",
            "McAfee",
            "AlienVault",
            "Cisco Talos"
        ]
        
        for i, service in enumerate(services, 1):
            print(f"   [{i}/{len(services)}] Проверка в {service}...")
            time.sleep(0.7)
        
        # Генерируем случайные результаты
        malicious_reports = random.randint(0, 3)
        total_services = len(services)
        
        print("\n📊 Результаты проверки:")
        print("-" * 30)
        print(f"   Проверено сервисов: {total_services}")
        print(f"   Подозрительных отчетов: {malicious_reports}")
        print(f"   Чистых отчетов: {total_services - malicious_reports}")
        
        # Оценка репутации
        if malicious_reports == 0:
            reputation = "Отличная"
            status = "🟢 Безопасный"
            recommendation = "IP-адрес безопасен"
        elif malicious_reports <= 1:
            reputation = "Хорошая"
            status = "🟡 Низкий риск"
            recommendation = "Наблюдение не требуется"
        elif malicious_reports <= 2:
            reputation = "Средняя"
            status = "🟠 Средний риск"
            recommendation = "Рекомендуется проверить активность"
        else:
            reputation = "Плохая"
            status = "🔴 Высокий риск"
            recommendation = "Рекомендуется блокировать"
        
        print(f"\n⭐ Репутация: {reputation}")
        print(f"   Статус: {status}")
        print(f"   Рекомендация: {recommendation}")
        
        return True
        
    except Exception as e:
        print(f"❌ Ошибка проверки репутации: {e}")
        return False

def show_reputation_services():
    """Показывает сервисы проверки репутации"""
    print("\n🌐 Сервисы проверки репутации:")
    print("-" * 35)
    print("   VirusTotal:")
    print("   • Анализирует более 70 антивирусами")
    print("   • Проверяет URL и файлы")
    print()
    print("   AbuseIPDB:")
    print("   • Сообщества по борьбе с абьюзом")
    print("   • Отслеживает вредоносные IP")
    print()
    print("   Spamhaus:")
    print("   • Борьба со спамом")
    print("   • Черные списки")

def show_safe_browsing_tips():
    """Показывает советы по безопасному серфингу"""
    print("\n💡 Советы по безопасному серфингу:")
    print("-" * 35)
    print("   • Регулярно проверяйте IP-адреса")
    print("   • Используйте VPN")
    print("   • Обновляйте антивирус")
    print("   • Избегайте подозрительных сайтов")
    print("   • Не скачивайте файлы с непроверенных источников")
    print("   • Используйте брандмауэр")

def main():
    """Главная функция проверки IP-репутации"""
    # Очищаем экран
    os.system('cls' if os.name == 'nt' else 'clear')
    
    # Показываем логотип и заголовок
    show_neodark_logo()
    print_header()
    
    try:
        # Показываем информацию
        show_reputation_info()
        
        # Показываем сервисы
        show_reputation_services()
        
        # Показываем советы
        show_safe_browsing_tips()
        
        # Демонстрация проверки
        print("\n" + "=" * 35)
        if check_ip_reputation_demo():
            print("\n🎉 Проверка репутации завершена!")
        else:
            print("\n⚠️  Проверка репутации не завершена")
        
        print(f"\n✅ Работа завершена!")
        print(f"⏰ Время завершения: {datetime.now().strftime('%Y-%m-%d %H:%M:%S')}")
        
    except KeyboardInterrupt:
        print(f"\n\n⚠️  Проверка прервана пользователем")
    except Exception as e:
        print(f"\n❌ Произошла ошибка: {str(e)}")
    
    input("\nНажмите Enter для выхода...")

if __name__ == "__main__":
    main()